#!/usr/bin/env bash
set -euo pipefail

rm -rf .built-assets

# Backing services (MySQL, Redis, OpenSearch, LocalStack) run via docker-compose.
# The app processes below run on the host inside the Nix dev shell.
docker compose up -d --wait

yarn install
hivemind Procfile.dev
